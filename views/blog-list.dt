extends base-blog

block body-blog
    - foreach (post; posts)
        a.post(href="/blog/"~post.link)
            p= post.title
            p.date #{post.date.day} #{post.date.month} #{post.date.year}
